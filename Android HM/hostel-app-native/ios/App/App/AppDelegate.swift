import UIKit
import Capacitor

@UIApplicationMain
class AppDelegate: CAPAppDelegate {
    override func initialize(_ application: UIApplication) {
        super.initialize(application)
        // Additional initialization code can be added here
    }
}