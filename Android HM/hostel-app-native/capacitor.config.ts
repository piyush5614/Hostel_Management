import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tchostel.app',
  appName: 'Hostel App',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;